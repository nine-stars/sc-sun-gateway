spring:
  application:
    name: sc-sun-gateway
  profiles:
    active: pre
  cloud:
    config:
      uri: http://localhost:9999
#      profile: dev,test,pre,prd

management:
  security:
    enabled: false

